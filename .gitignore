# OS files
Thumbs.db
.DS_Store
desktop.ini

# Office temp files
~$*
*.tmp

# Editor backups
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc

# PyInstaller build artifacts (microscopy GUI exe — built locally, never committed)
/build/
/dist/
*.spec.bak

# Claude Code personal config
.claude/settings.local.json

# Read-only probe outputs (may contain sample identifiers; not for commit)
_probes/
