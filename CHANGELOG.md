# Changelog

The single narrative history of the gjesus3 RDM pilot design + tooling; the numbered docs in [`mfb-rdm-docs/`](mfb-rdm-docs/) carry current state, not history; linked from [README.md](README.md) and [`00_INDEX.md`](mfb-rdm-docs/00_INDEX.md).

| Date | Author | Summary |
|------|--------|---------|
| 2026-08-10 | R. Tasseff | **Operators can now replace an existing recipe** (same branch as the row below; front-end only). Saving under a name that already existed gave a warning and a dead end. The backend has supported this all along — `POST /api/save_recipe` refuses the first write with `409 + {exists, file}` and writes when the same body is resent with `overwrite: true` — but `postJSON()` threw away everything except `data.error`, so the front-end never saw the `exists` flag and the operator got *"…or resend with overwrite=true"*, a sentence written for an API caller. **This was the UI failing to keep a promise it already made:** "Load recipe" has always said *"keep the name to overwrite it, or rename to save a copy"*, and keeping the name produced an error. Fix: `postJSON` now attaches `status` + the parsed body to the thrown `Error` (additive — every existing `catch … e.message` is unchanged; mirrored into `mri.js`'s copy so neither becomes the odd one out), and `#b-save` offers the replace on a 409, resending the **identical** body so what is written is exactly what the first attempt would have written. The result reports as **"Replaced"**, not "Saved". **The confirm names the *file*, not the typed name** — `"Study A"` and `"Study/A"` both sanitise to `study_a.yaml`, so someone who believes they are creating a new recipe sees which file is about to go — and it states two things that are easy to get wrong: recipes in the default folder are **on shared storage, so every operator of that instrument gets the new version** (it names the folder instead when the operator has pointed `recipes_dir` elsewhere), and **the replaced version is not kept**. No `.bak` — a deliberate call (Ryan, 2026-08-10) to keep the shared NAS folder clean, which is why the warning is explicit. `app.py` unchanged. **Verified** with 28 headless checks against the live app, with the recipes folder pointed at a scratch dir so no real recipe was touched: Cancel leaves the file **byte-identical and its mtime untouched**; OK replaces and both recipe pickers show the new description; load→edit→same-name→save replaces cleanly; a new name raises no confirm; an unusable name and other non-409 errors still surface their own message with no confirm; no `.bak` appears. The 77 checks from the row below re-ran green. **Exe rebuilt + REDEPLOYED** by the usual procedure (staged as `.exe.new`, atomic rename, checksum-verified, build dirs erased) — **live build is sha256 `cde997ba…`, 95,724,318 bytes**, superseding `c7f21a75…`; the single rollback backup stays the pre-branch 2026-08-02 exe (`ca2bd1c7…`, verified intact), so one restore undoes the whole branch. Validated **through the deployed exe**: all **105 checks** green plus the real 17-file dry run repeated (17/17, registry byte-identical). |
| 2026-08-10 | R. Tasseff | **Operator GUI usability: a reversible browse order, and one obvious "read the folder".** Two complaints from the microscopy operator, both in the shared GUI layer, so **both pages** (`/` and `/mri`) are fixed. **(A) The Browse… list can now be reversed.** A clickable `Name ▲/▼` column header, the Explorer idiom they asked for; folders always stay above files and only the name order flips. It matters because instrument source folders are named by date (`…\AxioScan\20260522`), so A→Z buries *today's* folder at the bottom of a list that grows by one per acquisition day — reverse-name **is** newest-first, the same reasoning the MRI SFTP exam list already used. **The order is applied in the backend** (`POST /api/listdir` takes `desc`) and sorted **before** the 3000-entry cap, because reversing the received list client-side would silently show the wrong end of a large folder — verified against a 3600-entry folder: the reversed view starts at the true last entry, not at entry 3000. The choice is remembered (localStorage), so it is one click ever, not one per launch. **The browser also reopens where each button was last left** (operator request, same day): order of precedence is the target box's own value → that button's last folder → the home folder, remembered **per target** because one modal serves the source folder, the RDM System root, the recipes folder and the MRI staging folder. A remembered folder that has since moved or been deleted falls back to home **silently** and is forgotten — a stale memory is not the operator's mistake — while a path they *typed* still errors normally. Every `localStorage` access is wrapped: a browser that blocks it loses the memory, never the browser. The browser modal itself — copy-pasted near-verbatim into `app.js` and `mri.js` — is now the single shared `static/folder_browser.js` (an IIFE exposing only `window.browseInto`; the two pages share one global scope, so a redeclared `$`/`esc` would kill the page). Its markup stays duplicated in both templates, same ids. **(B) "I picked a folder… now what do I press?"** Five differently-worded buttons did three overlapping things — including **two** carrying the identical label *"Show metadata labels from the folder"*, both buried in collapsed `<details>`, while Preview (the one the operator's instinct reached for) refreshed neither the palettes nor the filter dropdown. Now there are **two verbs on both pages**: **Read folder** (read the folder's metadata labels into *every* dependent surface) and **Preview** (show the resulting table) — and **Read folder happens by itself** when a folder is picked or typed, so in the normal case nobody presses anything. One `loadFromFolder()`, one `/api/discovered` call, refreshing both palettes + the filter dropdown + both sets of live examples; **Preview refreshes the same surfaces from its own response** (`cases[].discovered`, no second call), so pressing it can never leave anything stale. Also fixed the **structural** half: the always-visible Filter panel owned no loader — its dropdown was a side effect of two panels that can each be hidden (`#r-gaps` when the recipe leaves no gaps, `#r-meta` when the template has no `condition:` block), so a fully-specified recipe on a `condition:`-less template (e.g. `molecubes_ni_live`) would have left it with no way at all to populate. The one loader belongs to no panel, which retires that class of bug; verified with a no-gaps recipe (`#r-gaps` hidden, filter still fills). `/api/discovered` now also returns `blocking_errors` — a broken recipe used to yield an empty palette with no reason given, which only became visible once the read was automatic. Preserved deliberately: the builder still starts **blank** (a read loads discovered data only, never template defaults), WYSIWYG override semantics, narrow-only runner filter, and `runnerFilterUI.clear()` on recipe change. **Verified** headlessly against the live app, the real NAS and real AxioScan folders (jsdom running the pages' own scripts): 71 checks green across both pages, incl. a real 17-case Preview, and a **dry run of a real 17-file batch — 17/17 OK, `registry_raw.csv` byte-identical (same sha256) and `raw/` unchanged**. `test_value_fields.py` green. Branch `feat/gui-browse-sort-and-reload`. **Exe rebuilt + REDEPLOYED to `J:\gjesus3-data\tools\gjesus3_ingest.exe`** by the 2026-07-20 procedure: previous build backed up off-NAS at `C:\Users\rtasseff\temp\gjesus3_exe_backup_20260810\gjesus3_ingest.exe.pre-2026-08-10` (verified byte-for-byte, sha256 `ca2bd1c7…`), new build staged as `.exe.new` and **atomically renamed** (the tools dir was never without a working exe), deployed file checksum-verified against the local build, temp build dirs erased. Validated **through the deployed exe**: 63 checks green on both pages, and a real 17-file dry run (17/17 OK, registry byte-identical). Rebuilt + redeployed the same way after the reopen-last-folder addition — **live build is sha256 `c7f21a75…`, 95,721,155 bytes**; the one rollback backup (`…pre-2026-08-10`, sha256 `ca2bd1c7…`) is the pre-branch 2026-08-02 exe, verified intact after both deploys. Final validation through the deployed exe: **77 checks green** (63 + 14 for the folder memory) plus the real dry run repeated, registry still byte-identical. |
| 2026-08-07 | R. Tasseff | **This repo was relocated on the workstation — `projects\RDM\highCap\gjesus3-pilot` → `projects\DataInfra\gjesus3-archive\gjesus3-pilot`.** A filing move only: the NAS, the registry, and `J:\gjesus3-data` are untouched, and no tracked content changed. It is recorded here because a folder move silently drops four things that do not travel with it. **(1) Both worktrees were dead.** `git worktree list` read correctly from the main repo — git had already refreshed `.git/worktrees/*/gitdir` — but each worktree's *own* `.git` file still pointed at the old main-repo path, so any git command run **from inside** `gjesus3-dev\ni-live-hardening` or `gjesus3-dev\code-review-2026-08` failed with `fatal: not a git repository`. Fixed with **`git worktree repair`** from the main worktree (repairs both directions, touches no tracked files); both verified clean at their own HEADs afterwards. The lesson for any future move: check the worktree `.git` pointers, **not** `git worktree list` — the latter looks healthy while the worktrees are unusable. **(2) The one cross-repo coupling** called out in the 2026-07-24 row below — `$GJESUS3_REPO` in WorkstationOps' `config\finder-refresh.conf.ps1` — was left stale, which would have failed the nightly 03:00 Finder rebuild at "generator not found". Updated there, along with the two docs that repeat the path (README config table, `molecubes-tunnel` handoff reading list); `.\ops verify finder-refresh` passes all 6 checks against the new path. Committed in WorkstationOps as `34cd128`. **(3) Claude Code state is keyed by path** — `~\.claude\projects\<mangled-path>\` (agent memory + session transcripts) and the per-project entry in `~\.claude.json` are both per-path, so they had to be copied to the new key or the project starts blank. **(4) Sibling workstreams that cite this repo as read-only evidence** carried stale pointers and were swept: `DataInfra\image-server\README.md`, `DataInfra\misc\README.md`, `miniProjects\202605_SPRI\` (`CLAUDE.md`, `index.md`, `planning\project-definition.md`), and `DataInfra\gjesus3-tools\` (`CLAUDE.md` + `RESUME_HERE.md`, committed `a207aba`) — that last pair was the sharp one, since it actively told a session the archive path was "a stale, non-git copy — NOT authoritative yet" and would have sent work to a path that no longer exists. In-repo only [`tasks/dicom_regen_backfill_handoff.md`](tasks/dicom_regen_backfill_handoff.md) named the old location; it is **marked historical rather than rewritten**, since it also names a worktree that no longer exists. **Reading note:** this table is append-only, so every row above that mentions a `RDM\highCap` path is a stale reference to *this* repo, not a second copy. |
| 2026-08-06 | R. Tasseff | **Two synthetic NI acquisitions removed from true production — the first full reversal executed against the side-effect inventory, and it held.** `ACQ-20260212-CT-001` / `-002` were written into `J:\gjesus3-data` on **2026-06-29T16:05:51Z** by the end-to-end verification run for `53b022b` (`feat/ni-live-hardening`, "per-session corrections + tracer metadata"). The run used a synthetic source tree but passed `--nas-root J:\gjesus3-data` instead of a throwaway NAS; `tasks/ni_live_operator_plan.md` recorded it as a "throwaway nas", and that wrong write-up is why it sat unnoticed for **five weeks**. Not real data: `original_name` `1207/260212/0324_m61/20260212130722_CT/recon_{0,1}` corresponds to no real scan, `file_size_mb` `0.0`, `file_count` `1`, and each payload was literally the 5 bytes `DCM-0`. **Removed** (row numbers = [`10_TOOLS §2.1`](mfb-rdm-docs/10_TOOLS.md) side-effect inventory): both `raw/DICOM/2026/2026-02/<ACQ-ID>/` folders (#1); 2 rows from `registry_raw.csv` (#3, the commit point) and 2 from `ingest_manifest.csv` (#4); the newly-created subject `61-AE-biomaGUNE-0325` from `registry_subjects.csv` (#5 — `created == last_updated`, referenced by exactly these 2 acqs, so safe to drop rather than preserve); 2 rows from `pending_subject_metadata.csv` (#6, queued `pending-db` on a DB miss); and the **auto-created** project `PROJ-0051` / `AE-biomaGUNE-0325` in full — its `registry_projects.csv` row plus the whole `projects/AE-biomaGUNE-0325/` folder (`_project.yaml`, `provenance.csv` with both `FILE-000N` link rows, `raw_linked/` hard links, and the nightly-generated `index.html`) (#7/#8/#9/#10). `.acq_id_seq.json` **keeps** its `"ACQ-20260212-CT-": 2` reservation — per inventory row #2 ids are never reused, so the removal deliberately leaves a gap. **Method:** backup-first (all 6 registry files + both raw folders + the project folder copied off-NAS to `C:\Users\rtasseff\temp\gjesus3_ni_testdata_removal_20260806\` and byte-verified before any write), then **byte-exact line removal** — each CSV read as bytes with only non-matching lines rewritten through an atomic temp+replace, so the BOM, line endings and all 13,737 untouched rows survive verbatim (a `csv` round-trip would have rewritten every row). Dry-run first; every drop count matched the expected count exactly. **Verified:** 0 residual references to any of the four keys across all 6 registry CSVs; `registry_raw` 13,740→13,738 lines, `ingest_manifest` 13,740→13,738, `pending_subject_metadata` 291→289, `registry_subjects` 756→755, `registry_projects` 54→53; `validate_registries.py` reports **0 errors** (17,003 warnings, all the pre-existing `condition.is_control` enrichment sentinels). The global `registries/index.html` was left stale by design — the 03:00 WorkstationOps `finder-refresh` rebuilds it. **What this exercise showed about the inventory:** it is **complete** — the reversal used rows #1/#3/#4/#5/#6/#7/#8/#9/#10 and found nothing missing, including the auto-create path (row #10, present since `ed62eca`). A hand-off note claiming auto-creation was uncovered was **mistaken**; the real (minor) defect is that an auto-created project's side effects are spread across rows #7–#10 while #7 leads with "resolves to an **existing** project", which misled a careful reader. Both that clarity fix and the substantive follow-up — **nothing distinguished this run from a real ingest, so a production-`--nas-root` guard is warranted** — are filed in [`tasks/BACKLOG.md`](tasks/BACKLOG.md). |
| 2026-08-06 | R. Tasseff | **Remote access to the Molecubes acquisition box established (reverse SSH tunnel) — workstation half verified, box half staged.** New doc [`equipment/nuclear-imaging/live_machine_remote_access.md`](equipment/nuclear-imaging/live_machine_remote_access.md). **Why now:** two NI items require running commands *on* the box rather than on a copy of its data — **Gate-0** for live-box sync (`os.link` behaviour on the box's CIFS mount, the last gate before live-machine NI ingest goes live) and any refresh of `datapath.txt`, the 295,538-line dump that is the entire evidence base for [`live_machine_data_layout_and_sync_rules.md`](equipment/nuclear-imaging/live_machine_data_layout_and_sync_rules.md). Access to the box is rare and scheduled around imaging sessions, so each such question cost a physical slot and a failed attempt cost a week. **Shape:** the box is NAT'd behind its own router and cannot be reached inbound, so it dials **out** to the Data Office workstation (`10.10.2.195`) and we ride that connection back with `ssh -R`. The landing pad is **sshd inside WSL** plus a userspace Python forwarder, because the workstation account has no admin rights and cannot install a Windows SSH server; neither piece needs elevation. **Repo boundary** follows the `finder-refresh` precedent — the Windows half (WSL keepalive, forwarder, ports, health, logs) is the `molecubes-tunnel` operation in the separate `WorkstationOps` repo; this repo owns what the box is, why we reach it, and the operator procedure. The self-contained field card lives on the NAS (`S:\gnuclear\2026\Jesus\Ryan\tunnel.txt`) precisely because it must be readable from the box when nothing else is. **Safety on a live acquisition machine:** every action is classified persistent vs process-scoped; the only lasting changes are Remote Login (unavoidable) and the key + LaunchAgent (removable in three commands). `caffeinate` was **dropped** after `pmset -g` showed the box already never idle-sleeps — a redundant assertion is still a change. **Root cause of the first failed attempt, worth remembering:** `tcp_forward.py` passed `timeout=5` to `socket.create_connection` intending a connect timeout, but that leaves the socket in timeout mode, so it applied to every `recv()` — killing any connection idle for 5 s. Typing a password takes longer than that, so a transport bug presented as an authentication failure; the tell was that a *deliberately wrong* password produced the *same* error as the right one. HTTP had masked it for years (browsers silently reconnect), which is why the forwarder's original Django use never showed it. Consequence for procedure: **`nc -vz` is not a valid readiness test** against this forwarder — it succeeds whenever something accepts a socket, including when the far side is dead; read an SSH banner instead. **State:** firewall, routing, authentication, `-R` forwarding and idle survival all verified from the workstation; the box itself has not yet run the tunnel — that needs the next access window. Open questions NI-RA-01…05 in the new doc. |
| 2026-08-02 | R. Tasseff | **Project reference model: the "project hint" concept retired; a project now has exactly two identifiers, and its folder IS its name.** Operators found "project hint" confusing at the 2026-07-17 test, and they were right — it was never a hint. A project now has **`project_id`** (`PROJ-XXXX`, machine key, immutable) and **`name`** (human key: what the operator types, what everyone says, **and the folder name verbatim** — no `proj-` prefix, casing preserved, unique case-insensitively). Rename map: GUI label `Project hint` → **"Project name"**; config key `registry.project_hint` → **`registry.project_name`**; `registry_projects.short_name` → **`name`**; `registry_raw.project_hint` → **`project_id`**; `_project.yaml` `short_name:` → `name:`; link token `${project_hint}` → **`${project_id}`** (same value) plus a new **`${project_name}`**. **Hard cut, no deprecated alias** — a stale `registry.project_hint` now hits the resolver's unknown-key error listing the allowed keys, so an old config fails loudly instead of silently ingesting with no project; the 6 saved NAS recipes were **deleted, not migrated** (backed up first), since a recipe that errors on load is worse than one an operator rebuilds in a minute. **Three architecture findings this fixes** — (1) *a column name that lied*: `project_hint` had always stored resolved `PROJ-XXXX` ids, which cost two rewrites of the migration plan and nearly a needless 13,582-row value migration; (2) *the folder rule implemented three times* (`create_project`'s `proj-` prefix, `ingest_raw`'s `.lower()`, and `collisions.py` re-deriving `proj-<hint>` instead of reading the stored `folder_location`) — they had already drifted, which is why the GUI showed `AE-biomeGUNE` while the folder was `proj-ae-biomegune-0525`; (3) *no owning spec section*, so vocabulary drifted per layer and no single document was wrong. Fixes: the rule now lives only in new **`tools/ingest/project_naming.py`**; **[`05_PROJECTS §2a`](mfb-rdm-docs/05_PROJECTS.md) (Project Reference Model)** owns the concept with a consumer table; and **[`CONTRIBUTING-docs.md`](CONTRIBUTING-docs.md)** gains the forward rule — *one concept → one name → one owning section → one construction site* — with the requirement that the name be **true** (a `project_name` column holding ids would have been the same bug respelled). Resolution is now case-insensitive **and canonicalizing**: `resolve_project` returns `(project_id, name, folder_location)`, so an operator who types `ae-biomagune-1123` attaches to the existing project and gets `AE-biomaGUNE-1123` in link names and logs — no duplicate project. Spelling corrected to **`AE-biomaGUNE`** everywhere (it matches the animal facility's own `facility_id`s, 716/716 subject rows; `biomeGUNE` was a recipe typo). **Live migration executed 2026-08-02** in a no-ingest window via a dedicated dry-run-first, resumable, reversible script (`tools/migrate_project_naming.py`, kept in `tools/` as the paper trail): `registry_raw` **header-only** rename (values were already ids — the 7.2 MB of data was streamed through as opaque bytes and asserted byte-identical), `registry_projects` 51 rows re-pointed, **48 folders renamed**, 43 `_project.yaml` rewritten (edited as *bytes*, one line — 9 of the 43 are cp1252 and a decode round-trip would have mangled them), 6 recipes deleted. The 3 closed projects whose folders were already gone got record-only updates. Backup off-NAS at `C:\Users\rtasseff\temp\gjesus3_projectnaming_backup_20260802`. **Verified:** all 13,582 rows still join to a project; zero `proj-*` folders remain; every `folder_location` == `/projects/<name>/`; **134/134 acquisition/link pairs across all 51 projects confirmed to be the same file** (hard links intact — note that `st_nlink` and `fsutil hardlink list` are BOTH unsupported over this SMB share, so the check compares file identity, pairing each link to its acquisition via `provenance.csv`); Finder regenerated (13,582 acqs, 43 per-project indexes, 8 closed correctly skipped). **Operator exe rebuilt and redeployed** to `\\GJESUS3\...\tools\` (checksum-verified; previous build kept as `.old_20260802`) in the same window — required, because `registry.py`'s header check makes the old exe hard-fail the append (loudly, not corruptingly) the moment the header flips. **Smoke-tested through the deployed exe**: a real non-dry-run AxioScan ingest against the migrated schema resolved `ae-biomagune-1123` → `PROJ-0014 (AE-biomaGUNE-1123)`, appended cleanly to the new 28-column header, hard-linked into the **new** folder name with no `proj-` folder created, refreshed the project index, and hit the animal-facility DB for subject enrichment — run against a scratch NAS seeded with the migrated live registries, so **production `/raw` was never written to**. Discrepancy found and recorded: only **43** `_project.yaml` exist, not the 48 the plan assumed — the 5 closed-but-present folders lost theirs at close-out. Deliberately out of scope: deleting those 5 folders, the semantic re-projecting of person/topic projects ([`tasks/BACKLOG.md`](tasks/BACKLOG.md) / PROJ-05 — this migration is mechanical normalization only), and the publications / curated-datasets registries, which keep their own unrelated `short_name`. |
| 2026-07-24 | R. Tasseff | **Scheduled global Finder rebuild handed to WorkstationOps (a separate workstation-ops app); this repo keeps only the generator.** The daily global rebuild had been an interim standalone `schtasks` task running `tools/scheduled_finder_refresh.bat` — a shim that lived outside any tracked tooling, **swallowed the generator's exit code** (a failed rebuild looked identical to a clean one), had no overdue/health signal, and logged to an unrotated file nobody watched. Ownership of the *orchestration* moved to **`WorkstationOps`** (`C:\Users\rtasseff\OneDrive - CIC biomaGUNE\WorkstationOps`, its own repo) as a new `finder-refresh` operation, modeled on the existing `vps-backup` op: it shells out to the pinned Store-Python 3.13 (full path, not the WindowsApps `python` alias — Task Scheduler PATH is unreliable), checks the real exit code, sets a pending flag + tray notification on a failed scheduled run, probes NAS reachability with a bounded `Test-Path` (a wedged share skips cleanly instead of hanging), and exposes a health signal via `.\ops status finder-refresh` — the published `index.html` mtime (on the NAS only the generator writes that file, so its mtime is the last successful publish; older than 30h reports STALE). Scheduled **daily 03:00** (safe here — the QNAP is always mounted, unlike the `X:` share that pushed `vps-backup` to 05:00). **The boundary:** this repo owns only `tools/generate_index.py` (how a Finder is built from the registry); WorkstationOps invokes it directly, no `.bat` shim. The single cross-repo coupling is `$GJESUS3_REPO` in WorkstationOps' `config\finder-refresh.conf.ps1` — **if this repo is ever moved on the workstation, that one line must be updated**; `.\ops verify finder-refresh` guards it and fails loudly. Validated end-to-end (verify incl. a side-effect-free generator import check, status, `--dry-run`, and one real rebuild). Committed in WorkstationOps as `8759673`. Docs here updated: new [`11_OPERATIONS §5.6`](mfb-rdm-docs/11_OPERATIONS.md) (authoritative — where it runs, the health check, the repo-move interdependency), [`tools/FINDER.md`](tools/FINDER.md) *Keeping it fresh* (dropped the superseded `schtasks` one-liner), and [`tasks/STATUS.md`](tasks/STATUS.md) §2. **Cutover completed 2026-07-24:** the interim `gjesus3 Finder refresh` 05:00 task was unregistered, `WorkstationOps-finder-refresh` scheduled at 03:00 (verified Ready), and `tools/scheduled_finder_refresh.bat` deleted. |
| 2026-07-20 | R. Tasseff | **Operator-GUI polish + per-project Finder refresh on GUI ingest — branch `feat/gui-operator-polish`, merged to `main` (`97500cb`); exe rebuilt + redeployed + validated in production.** Four fixes from the 2026-07-17 microscopy operator test. **(1) Full metadata-token palette:** the runner/builder palettes offered only `discovered.*` filename chunks (+3 hard-coded builder extras), so `original_name` / `instrument` / `operator` / … were undraggable even though they resolve at ingest; now the **full resolver context** is offered in all three microscopy palettes + the MRI page, sourced from **one** list (`resolver.link_filename_token_fields()`, new `GET /api/link_tokens`) so the palette and the resolver can never drift. Traced (in `config.py`) that these flat tokens resolve **only** in `link_filename` — the `registry.*` / `operator` / `condition.*` fields go through `resolve_value` (`discovered.*`-only) — and made the client example-resolvers render a recognised-but-uncomputable token as a `<placeholder>` instead of a false "unresolved" flag. **(2) "NAS" → "RDM System"** across every operator-visible string (both templates, `app.js` / `mri.js` status + error strings, both help pages, the two `app.py` `jsonify` errors) + a `GLOSSARY.md` definition; internal identifiers (`nas_root`, `/api/nas_root`, `nasInput`, comments) untouched. One residual left deliberately: the shared-core `env.NasRootError` message (also CLI/data-office-facing). **(3) Completion modal** on a real (non-dry-run) ingest, both pages — new shared `static/completion_modal.js` (`window.showCompletionModal`); green ✓ "Ingest complete" / red "with issues" + failed ACQ-IDs; dismissible (button / `Esc` / backdrop), focus moved in and restored. **(4) Per-project Finder refresh on GUI ingest** (the index-refresh addendum): the GUI calls the ingest functions directly and bypassed `ingest_raw.main`'s auto-refresh, so a GUI upload never refreshed any index (why a researcher's upload was missing from the project index); the ingest worker (`_ingest_sse_response`, both `/api/ingest` + `/api/mri/ingest`) now regenerates **only** the touched project(s)' `index.html` (targeted `--project`, reusing `ingest_raw._touched_project_hints`), never the ~18 MB global index (the scheduled job owns that), best-effort. Merged `main`'s finder-refresh foundation into the branch; the PyInstaller spec now bundles `generate_index.py` + `find_acq.py` (datas + hiddenimports). **Deploy:** rebuilt `gjesus3_ingest.exe` to an off-OneDrive temp dir, backup-first redeploy to `\\GJESUS3\gjesus3\gjesus3-data\tools\` (old exe + all registry CSVs backed up off-NAS byte-verified first; deployed via a staged `.new` copy + atomic rename because a transient SMB lock blocked the in-place overwrite — the tools dir was never without a working exe), checksum-verified byte-identical, temp build erased. **Validated in production through the deployed exe** with a real AxioScan ingest of one animal `.czi` (`MFB_AUA_1123_ID298li`) into the **existing** PROJ-0014 — all four fixes confirmed live (tokens visible, zero "NAS" text, completion modal shown, and the project `index.html` hash + mtime changed with the new acq present = the refresh fired end-to-end in-frozen), then **fully removed** (Data-Office manual, backup-first, byte-exact line removal) with every count returned to baseline. The removal surfaced a place worth documenting — the hidden `.acq_id_seq.json` ACQ-ID reservation (ids are never auto-reused) — which motivated the doc below. **Docs:** [10_TOOLS §2.1](mfb-rdm-docs/10_TOOLS.md) gained a **Side-effect inventory** — the complete list of every file & row a single ingest writes (incl. the easily-missed `registry_subjects.csv` upsert, the `pending_subject_metadata.csv` DB-miss row, the project `provenance.csv` row, and the `.acq_id_seq.json` reservation) with how to reverse each — cross-referenced from [`INGEST_CLI.md`](tools/INGEST_CLI.md). Merged to `main` as `97500cb` (`--no-ff`); `refactor/project-naming` (P) has already been rebased onto it, and the temporary `gui_operator_polish_handoff.md` + addendum notes were dropped on landing (per the 2026-07-17 drop-temp-handoffs precedent). |
| 2026-07-20 | R. Tasseff | **Researcher Finder refresh reworked — targeted per-project on ingest + a scheduled global rebuild, replacing the wholesale-on-every-ingest regen.** The old auto-refresh lived only in `ingest_raw.main()`, so operator **GUI** ingests (which call the pipeline directly and bypass `main()`) **never refreshed the index at all** — a researcher's just-uploaded scan was missing from the project `index.html` (surfaced 2026-07-17); the CLI path meanwhile rebuilt the whole ~18 MB global page + all 43 per-project pages on every ingest (heavy, race-prone under concurrent writes, and it silently only ever ran on the CLI). Split by cost: **(1)** `generate_index.py` gains a **targeted `--project ID`** mode — regenerates only that project's `index.html`, skipping the global rebuild; `ID` matches a PROJ-id / short_name / folder name, keyed on the stored `project_hint` (which is the resolved **`PROJ-XXXX` id** — verified across all 51 live projects, 0 slugs). **(2)** the `ingest_raw.py` auto-refresh is now **opt-in** — `--refresh-index {none,projects,full}`, default `none`; `projects` regenerates only the run's touched project(s) (new `_touched_project_hints` maps the run's acq_ids → their PROJ-ids from the registry), `full` is the old global+all-per-project. **(3)** the **global** index is kept fresh by a **scheduled job** — new `tools/scheduled_finder_refresh.bat` calls the generator against the **UNC** share root (not a mapped drive, so it runs from a scheduled/logged-out context; logs to `%LOCALAPPDATA%\gjesus3\`), registered daily via Task Scheduler on the data-office workstation (`schtasks` command in `tools/FINDER.md`). **(4)** the **operator GUI** will regenerate just the touched project's index right after an ingest — the immediacy path researchers value — wired in the `feat/gui-operator-polish` addendum, shipping with that branch's next exe rebuild (needs `generate_index.py` + `find_acq.py` added to the PyInstaller spec). Docs swept for the stale "auto-refreshes on every ingest" claim: `tools/FINDER.md` (new **"Keeping it fresh"** section), `tools/INDEX.md` / `INGEST_CLI.md` / `OPERATOR_FAQ.md`, `10_TOOLS.md` pipeline step 14 + diagram, `GLOSSARY.md`, `tasks/STATUS.md`, `00_INDEX.md`, `01_OVERVIEW.md` / `03_RAW_STORAGE.md` / `05_PROJECTS.md`, and the six `equipment/…workflow_notes.md`. UNC read verified (13,582 rows); targeted mode verified against the live registry. See [`tools/FINDER.md`](tools/FINDER.md). |
| 2026-07-17 | R. Tasseff | **Operator-GUI fixes shipped + verified in production; the frozen `.exe` rebuilt and redeployed.** Two branches merged to `main` and the fixed exe deployed to `\\gjesus3\…\tools\` (old exe + all registry CSVs backed up byte-verified off-NAS first). **(1) Frozen-exe resource loads** (`fix/gui-frozen-exe-resources`): the packaged `gjesus3_ingest.exe` had **never completed a real committed ingest** — it crashed at README generation because the `ingest/` layer resolved bundled data with a naïve `dirname(__file__)` path (not `sys._MEIPASS`-aware) and `README_raw.txt` (plus `project.yaml` and all of `tools/reference/`) was never in the PyInstaller spec. Fixed with a frozen-aware `ingest/resources.py` resolver mirroring `operator/templates.py`, routing the **three** naïve loaders through it — `readme.py` (the hard crash) and two *guarded* siblings an independent sweep caught that the original handoff missed: `create_project.py` (wrote a non-templated `_project.yaml`) and `anatomy_derive.py` (silently derived **no** microscopy organ-anatomy in the frozen build) — plus bundling the data files. **(2) Recipe override semantics** (`fix/microscopy-gui-filters-and-gaps`): a recipe could display one config in the GUI but ingest another — emptying a structural field in the builder made the recipe omit it, so the template silently reasserted at ingest (WYSIWYG broken). Independently reviewed ([`tools/operator/gui/microscopy_gui_override_semantics_review.md`](tools/operator/gui/microscopy_gui_override_semantics_review.md); the review confirmed `config_builder`'s merge was already correct — the defect was UI-only) and fixed: the builder now writes structural keys **explicitly** (an emptied filter saves as `{}` and CLEARS the template's `group_code=MFB`, enabling a collaboration recipe, while keeping the blank-start builder), and the runner fetches + shows the **effective** (template+recipe) filter via a new `/api/effective_config`, ANDing operator conditions onto it (narrow-only — it can never silently drop MFB). Also unified the drifting builder/runner value-field catalogue (`tools/operator/value_fields.py` — fixes `registry.session_id` being un-promptable). **(3) Verified end-to-end in production:** rebuilt the exe from `main` (checksum-verified byte-identical after deploy), then ran a real **9-acquisition AxioScan ingest through the deployed exe** — all 9 wrote `README.txt` (the exact former crash point) and auto-derived `region=lung` from the organ map, confirming **both** the README and organ-map fixes work in-frozen. The 9 smoke-test acqs were then surgically removed from `/raw/` + `registry_raw.csv` + `ingest_manifest.csv` (line-based, backup-first; the shared subject rows were left intact — subjects `65/70-AE-biomaGUNE-1321` also carry real acqs; registry back to **13,557**). **Found, not fixed:** the Finder `index.html` was not auto-refreshed by this ingest (stale at 2026-07-14) — carried for a look. Both feature branches merged + deleted (local + origin) and their worktrees cleaned up. |
| 2026-07-16 | R. Tasseff | **No-DICOM MRI backfill executed — the worklist is drained; the deferred-regeneration procedure is now official policy.** Branch `feat/dicom-regen-backfill`. **(1) The design call** (the 2026-07-15 blocker): "fill an already-registered acquisition" is a **standalone backfill on the `recover_subject_metadata` recovery pattern** — an in-place update that **keeps the ACQ-ID** — never a re-ingest (the dedup keys on `(acq_date, original_name)` registry presence and skips every registered placeholder; bypassing it would mint a duplicate ACQ-ID from inside `ingest_single`). Enabled by the one genuinely new capability: **`registry.update_row`** — the toolset's first in-place registry write (header-checked read-all/atomic-rewrite under the caller's `registry_lock`, `only_if_blank` controlled-write; unit-tested). **(2) Why 612 could never drain to 153 pending:** worklist triage against the staged kenia mirror (121 studies / 43.11 GB on `D:`) split the 247 `pending` into **153 backfillable** (have `pdata/<idx>/2dseq`) and **94 with no reconstructable source** (80 header-only aborted/never-reconstructed + 14 `fid`-only raw k-space) — the latter now carry the new terminal worklist status **`no-source`** (a status *value*, not a column — the frozen GUI's merge preserves unknown values but a new column would break its header check; set only via the human-gated `--mark-no-source` pass). The §4.4 classifier question closed **against widening**: every no-source exam is an ordinary image sequence (FLASH/FcFLASH/MSME/RARE/UTE3D/DTI/…) — `specpar`/`AdjStatePerScan` coexist with plain image exams and are not non-image signals. **(3) A third Dicomifier bug — why these 153 specifically were stuck:** all 153 are plain single 3D volumes stored `disk_reverse_slice_order` (uniform: `VisuCoreDim=3`, `VisuCoreFrameCount=1`, no `VisuFGOrderDesc`), and Dicomifier 2.5.3 (and current upstream master) **crashes on exactly that shape** (`convert.to_2d` synthesizes FG_SLICE as the only frame group → `volume_groups` empty → `cumprod([])[-1]` IndexError) — the stock converter converts **0 of 153** (11/11 probe). Diagnosis also exposed that upstream's slice-order flip is a **silent no-op** (`numpy.flip` result discarded). Both fixed by **workaround #3**, [`tools/ingest/dicomifier_driver.py`](tools/ingest/dicomifier_driver.py): a verbatim copy of `get_pixel_data` with the two fixes, `__code__`-swapped before dispatching to the stock CLI (module tables bind the function object at import), self-disabling when upstream fixes land; `paravision_regen` now routes through it. **Pixel-level validated** against the raw `2dseq` (all 64 frames == disk slices exactly reversed; positions ascend uniformly from the origin; the unflipped comparison fails). Blast-radius check: a 157-exam sample of the 3,297 previously-regenerated acqs found **zero** `disk_reverse` sources — existing NAS data unaffected. Upstream issue draft ready to file. **(4) The run** (new [`tools/backfill_dicom_regen.py`](tools/backfill_dicom_regen.py); dry-run default, verify-after-write with rollback, reconcile path for crash recovery, per-exam error isolation; registry update + worklist flip inside ONE lock section): pilot 1 exam → deep-verified → batch. **All 153 regenerated** (17,122 DICOMs / 1.64 GB deposited; two exams hit a transient WSL/CIFS `EACCES` on the registry `os.replace` *after* their DICOMs were verified — the reconcile path completed them from Windows, proving the crash-recovery design). Per row: DICOMs into the existing `.data/`, `checksums.json` rebuilt, blank-only sidecar age refill, 3-line README refresh, registry `file_count`/`file_size_mb`/`checksum_present` + blank-only `acquisition_datetime` (from the regenerated StudyDate) + `file_format`. **Registry still exactly 13,557 rows — zero duplicates**; the **78 blank `acquisition_datetime`** rows are all real now, closing the last of the 2026-07-15 age bug (every regenerated sidecar with a `date_of_birth` has its age; 67 lack a dob — that is the `pending-db` recovery lane, not this one). Worklist final: **153 `regenerated` / 365 `not-applicable` / 94 `no-source` / 0 `pending`**; invariant (`backfill_pending_dicom --dry-run` → 0 to add) holds. **(5) Project links:** `relink_mri_regen.py` from Windows created **663** hard links, 0 errors — our 153 **plus 510 pre-existing empty shells the 2026-06-14 relink had left behind** (same WSL-EPERM damage class, discovered by this pass); the remaining 2 candidates are the two known same-animal/same-day link-name collision pairs (`MRI_m23_0219_20220124_{1,3}_1`, BACKLOG item) — skipped, not merged. Spot-verified: 64/64 files, same inode as `/raw`. **(6) Docs:** the deferred-regeneration procedure is now official — [`10_TOOLS §3.8`](mfb-rdm-docs/10_TOOLS.md) (tool + worklist status domain + `update_row`) and [`11_OPERATIONS §5.5`](mfb-rdm-docs/11_OPERATIONS.md) (the data-office run, WSL env, relink step, human-gated no-source pass); the **false idempotency claim** (a re-run "fills the `.data/`" — disproven 612/612) is corrected at every site that carried it, including the operator runbook §2/§6 **before** it was archived to [`tasks/archive/mri_no_dicom_regeneration_runbook.md`](tasks/archive/mri_no_dicom_regeneration_runbook.md) (the `equipment/` boundary call — it documents our tooling, not platform reality); ~10 inbound links repointed at the new official homes (the 2026-06-12 row below keeps its stale link — this table is append-only). Registry CSVs were backed up byte-verified off-NAS (`gjesus3_backfill_backup_20260716`) before the first production write. |
| 2026-07-15 | R. Tasseff | **`age_at_acquisition` no longer derived from a placeholder date; the 612 DICOM-less MRI placeholders enrolled in the regen worklist.** Two findings from the 2026-07-14 pass, both closed. **(1) Fabricated ages (commit `f567fae`).** 92 no-DICOM MRI acquisitions carried an `age_at_acquisition` measured to their **ingest** date — `ACQ-20260613-MRI-001`, dob 2021-06-09, `P1830D`, landing exactly on its 2026-06-13 registration date. The bad kind of wrong: not a null or a sentinel, but a plausible number in a DB-sourced field that reads as authoritative. Chain: [`ingest_raw.py`](tools/ingest_raw.py) Step 3 falls back to `datetime.now()` for the ACQ-ID prefix when there is no `acquisition_datetime` **and** no DICOM StudyDate (a documented placeholder — it WARNs "backfill when known" and leaves the registry column blank), then passed that same `acq_date` to `enrichment.build_enrichment`, whose `_acq_for_age` treats a bare `YYYYMMDD` as real. **Fixed at the call site, not in `_acq_for_age`** — that fallback is *correct* for the DICOM-StudyDate branch (a real discovered date, where the local `acq_dt_iso` is still empty because that branch backfills the cfg rather than the local), and by the time both reach `_acq_for_age` they are indistinguishable, so only the caller knows which it holds. No date in → `age_iso8601` returns None → age stays blank, which is honest. `test_phase3_enrichment.test_age_needs_a_real_date` pins **both** halves (no date → blank; real discovered date → still derives) so nobody later "simplifies" `_acq_for_age` and breaks the StudyDate path. **Blast radius established by rescanning all 13,557 sidecars** — each age recomputed from its own `date_of_birth` against the registry's `acquisition_datetime`: exactly **92 wrong** (all MRI, all `file_count=0`, all blank `acquisition_datetime`) and **0 disagreements among the 10,666** rows that have a real date. Those 92 are blanked on the NAS (age only; `date_of_birth` and all else untouched; backup `gjesus3_age_blank_backup_20260715`); a rescan now reports 0. **(2) The bigger gap — 612 unregistered DICOM-less acquisitions.** The invariant *no DICOM-less acquisition should exist without being registered as needing a Dicomifier pass* did not hold: `registries/pending_dicom_regen.csv` **had never been created**. Not a leak — the auto-queue landed 2026-06-24 (`6dce5f9`) and every placeholder was ingested 2026-06-13/14, ten days earlier; no MRI has been ingested since. But it left **612** empty `<ACQ-ID>.data/` placeholders (both `mri_jrc_*_regen.yaml` configs, `auto_regenerate_dicom: true` — Dicomifier simply wasn't available on that box) tracked nowhere; the 92 were only the subset the age bug happened to expose. New [`tools/backfill_pending_dicom.py`](tools/backfill_pending_dicom.py) enrols them, writing the same rows the live path would have: `original_name` (the ParaVision `<study>/<exam>` kenia identity) / `canonical_path` / `ingest_config` from the registry, `paravision_version` from each sidecar (6.0.1 × 277, 7.0.0 × 335 — no gaps), `reconstructions` from the producing config. It trusts the **disk**, not `file_count` (only enrols a genuinely empty `.data/`), and is idempotent + status-preserving (verified: a re-run adds 0, duplicates nothing, and a row marked `regenerated` survives). Worklist now **612 rows, all `pending`**; invariant verified holding (612 DICOM-less, 612 registered, 0 unregistered, 0 false enrolments). The tool doubles as the invariant's checker — `--dry-run` reports 0 to add when it holds. Draining the worklist fixes both problems at once: real DICOMs *and* a real acquisition date, hence a correct age. |
| 2026-07-14 | R. Tasseff | **Live-NAS backfill + retention close-out — researcher names, real project dates, 8 projects closed.** A **direct write to true production** (`J:\gjesus3-data`), explicitly authorised for this pass; everything touched was backed up off-NAS first to `C:\Users\rtasseff\temp\gjesus3_backfill_backup_20260714\` (both registries, the 1,915 rewritten sidecars, and the deleted projects' `provenance.csv` / `_project.yaml` / `raw_linked` inventories). **(1) `researcher` backfilled on 1,915 acquisitions** across 15 projects, into **both** the `metadata.json` `user_supplied.researcher` sidecar **and** the `registry_raw.csv` column. `researcher` is required but post-dates most historical ingest; where a project's name carries a person the name was recovered from there — the join is `registry_raw.project_hint` → `registry_projects.short_name` (**`project_hint` holds `PROJ-00NN` ids, not names**). Value convention = **lowercase first name**, matching the pre-existing `irene` rows: claudia 715, itziar 552, laura 461, susana 78, elena 63, marina 17, irene 16, sergio 10, irantzu 3. Zero conflicts (no row carried a differing researcher); **11,508 rows remain blank** — their project names name no person. **(2) `registry_projects.csv` `start_date` / `last_activity` now mean *acquisition* dates, not ingest dates** — all 51 rows previously carried a uniform 2026-06-1x ingest date. Derived per project from `provenance.csv` `input_refs` → `registry_raw.acquisition_datetime`, i.e. what is genuinely hard-linked under `raw_linked/`. **(3) Retention close-out — 8 projects** whose newest acquisition is **older than 3 years** (`PROJ-0003/0005/0006/0007/0008/0009/0016/0017`, all 2022 NMR-platform studies): **project folder deleted**, registry row **retained with `status=closed`**. Safe because project links are hard links ([10_TOOLS §2.1.1](mfb-rdm-docs/10_TOOLS.md)) — deleting a link never touches `/raw/`; all 2,684 links were **inode-verified** against a surviving `/raw/` copy beforehand (`st_nlink` is unreliable over SMB — compare `st_ino`), and **all 13,557 `/raw/` acquisition folders verified intact** afterwards. The 92 `file_count=0` no-DICOM MRI rows have a genuinely blank `acquisition_datetime` and are excluded from the date derivation (no date, no data); their inclusion would have spared 5 of the 8 closures. **(4) Finder fix** (commit `9a0b37b`): the "All instruments" view appeared to hold only CELL+MRI — the *search* was never limited (all 6 instruments were always in the data and the dropdown); the cause was the `CAP = 800` render limit against the newest-first default sort, so LSM9/ZWSI/PET/CT fell outside the rendered window. Added a **"Show all N"** button, a clearer count, and rewrote rendering (delegated listener + single `innerHTML`) so 13.5k rows stay usable; `generate_index.py` now **skips `status=closed` projects** so a regeneration cannot resurrect a deleted folder. Global + 43 per-project `index.html` regenerated. **Found, not fixed:** `age_at_acquisition` is wrong on those 92 no-DICOM MRI rows — it appears derived against the *ingest* date when `acquisition_datetime` is blank (`ACQ-20260613-MRI-001`: dob 2021-06-09 → `P1830D` = exactly the 2026-06-13 registration date). Carried in [`tasks/STATUS.md`](tasks/STATUS.md) §2 pending triage. **Leftover:** the emptied `projects/proj-ae-biomegune-0219/` directory (PROJ-0017) resists `rmdir` with `WinError 5` via both `J:` and UNC and needs NAS-side action — harmless (its 14,911 files are gone and the tooling skips closed projects). |
| 2026-07-12 | R. Tasseff | **Integrity-cluster fixes from the 2026-07-08 review triage — branch `fix/integrity-cluster-2026-07` (pending review/merge).** The unambiguous, agreed-solution subset of the triaged findings, done on a branch with isolated unit tests (no live-NAS operations). **Code:** (1) `pending.py` recovery-queue write is now **atomic** (temp+`os.replace`, mirroring `pending_dicom.py`) and **serialized under the registry lock** — a crash mid-write can no longer truncate the live 250-row queue, and two concurrent DB-miss ingests can't lost-update it (review §3.1 #2, HIGH); (2) the copy-phase verify-failure early-returns in `ingest_raw.py` now **roll back the partial acquisition folder** (`_rollback_uncommitted`) instead of orphaning it before the commit-phase `try/finally` (§3.1 #3, HIGH); (3) `committed = True` moved **inside the registry lock, right after `append_row`** (the true commit point), closing the Ctrl-C window that could roll back an already-written row (§3.1 #4); (4) registry `checksum_present` now reflects reality — **"N" for the empty no-DICOM MRI placeholder** instead of a hardcoded "Y" (§3.1 #9). New `tools/test_pending.py` + a `checksum_present` case in `test_registry_fields.py`; full hand-rolled suite green. **Docs (catch-up to code, §2.4):** fixed the two stale `10_TOOLS.md` examples that put `operator:` inside the `registry:` block (→ `researcher:`, the current required column — the pre-rename form is rejected by the resolver); documented the `auto_discover.subject_parse` block ([10_TOOLS §2.1.6](mfb-rdm-docs/10_TOOLS.md)) and the multi-animal `subjects:[]` sidecar array ([08_METADATA §4.4](mfb-rdm-docs/08_METADATA.md)); added `rebuild_baseline/README.md` flagging that its `registry_raw.csv` is a stale 24-column pre-restart header. Deferred (needs design/env input, still in BACKLOG): the pre-lock dedup snapshot (§3.1 #1), decoupling enrichment from the live DB (§3.2.3), the DR purchase, and scheduling the validators. |
| 2026-07-08 | R. Tasseff | **Architecture + code review of the whole system** (at commit `a6de67d`, branch `main`). Full immutable report: [`tasks/archive/2026-07-08_architecture_code_review.md`](tasks/archive/2026-07-08_architecture_code_review.md); actionable follow-through filed to [`tasks/BACKLOG.md`](tasks/BACKLOG.md) ("Architecture & code review follow-through"). **Verdict:** the core is disciplined and right-sized — *not* broadly over-engineered; the registry-schema integrity mirror is fully consistent (spec ↔ `registry.py` ↔ live NAS header). Real issues: (1) **no DR** — single NAS, RAID 5 / 20 TB, no off-array copy, true production = the #1 risk (inaction, not design; plan already in [02 §5.4](mfb-rdm-docs/02_INFRASTRUCTURE.md)); (2) three HIGH latent bugs in the concurrent-write / partial-failure paths (pre-lock dedup snapshot → double-ingest; non-atomic unlocked `pending.py` rewrite; un-rolled-back orphan folders); (3) edge sprawl — ~10 spent one-off scripts + 3–4 verbatim copies of the registry-rewrite block; (4) heaviest over-engineering is the doc-governance apparatus, not the code; (5) the embedded-HTML Finder won't scale to ~100 k rows. Divergence feared in the schema was **doc lag** ([10_TOOLS §2.1](mfb-rdm-docs/10_TOOLS.md) still shows the pre-rename `operator:` in the `registry:` block), not code drift. Findings are a reviewer snapshot pending a triage pass (BACKLOG top task). |
| 2026-06-24 | R. Tasseff | **Operator GUI rollout — `gjesus3_ingest`, microscopy + MRI, frozen .exe on the NAS.** Added a deliberately-simple **MRI page** (`/mri`) to the existing microscopy GUI (still `/`), over the same validated pipeline (no reimplementation). MRI flow: **list studies on the scanner → pull → preview → ingest**, with only two operator-editable fields (destination project + token link-name). **Source = SFTP pull from `kenia`, READ-ONLY** — only `listdir/stat/get`, no write path anywhere (audited); creds from `~/.ssh/gjesus3_mri.cred` (`ftp_mirror.py`/`operator/env.py` read it, env vars override); staging to `<NAS>/staging` is invisible + auto-deleted after a clean ingest; **`instrument_model` auto-derived** from `acqp ACQ_station` (`mri_station`/`mri_scanner_model`, [09_MODALITIES §1.4](mfb-rdm-docs/09_MODALITIES.md)); destination NAS validated before the pull; in-batch + on-NAS link-collision warnings (`operator/collisions.py`); default-on Dicomifier regen (graceful); non-scan ParaVision sibling folders skipped with a clean message. Frozen to a single Windows `.exe` (`tools/operator/gui/gjesus3_ingest.spec`; `paramiko` hidden-import + bundled `ftp_mirror.py`; **build OUTSIDE OneDrive**) and deployed to **`\\gjesus3\gjesus3\gjesus3-data\tools\`** (one exe + two UNC shortcuts, the MRI one passing `--mri`, + novice HTML guides reachable via an in-app "? Help" link). Smoke-tested frozen incl. a live read-only SFTP listing of 1083 studies through the exe. Branch `feat/mri-operator`→`feat/operator-rollout`, merged to `main`. See [10_TOOLS §5.2](mfb-rdm-docs/10_TOOLS.md). |
| 2026-06-23 | R. Tasseff | **Researcher Finder enhancements (branch `feat/finder-mvp`, merged to `main`).** Built at the data office's request on top of the parked Finder MVP: (1) **auto-refresh on ingest** — a successful (non-dry-run) `ingest_raw.py` batch regenerates `registries/index.html` non-fatally (refresh failure → WARN, never fails the ingest); manual `generate_index.py` still works; (2) **expanded table columns** (in order: Acq ID, Date, Instrument, Modality, Researcher, Operator, Sample, Subject, Organism, Sample type, Original name, **Project = project `short_name`**, **Owner = project owner**); (3) **detail panel** now shows **File size (MB)** + **Project description**, with **Copy-path buttons** for the metadata / raw / project paths (pasting the `metadata.json` path into Chrome/Edge renders the sidecar as a native collapsible JSON tree); (4) **`DEFAULT_LINK_BASE` fix** — corrected to the portable container UNC `\\GJESUS3\gjesus3\gjesus3-data` (the old default omitted the `gjesus3-data` component → unresolvable paths). `index.html` documented as a generated artifact in `registries/`. See [`tools/FINDER.md`](tools/FINDER.md), [06_REGISTRIES §1.2](mfb-rdm-docs/06_REGISTRIES.md). |
| 2026-06-15 | R. Tasseff | **2026-06 code-review correction pass merged to `main`** (folded to one commit; source branch `review/code-inspection-2026-06-12` retired → tag `archive/review-code-inspection-2026-06-12`). **HIGH:** `extract_xmri_archives` accepted a partial/interrupted extraction as complete on resume (now `.extracted` sentinel + partial-dir cleanup + working `--force`); `extract_ni_archives` dead `--force` now re-extracts; NI+MRI slim-copy planners fail loudly on destination-name collisions (would have silently overwritten). **MEDIUM:** registry-lock stale-break TOCTOU fixed (steal-by-rename + age re-check); pre-flight `registry.assert_header_compatible` before the expensive copy (so a pending migration fails fast instead of after a multi-GB copy the rollback would delete); microscopy folder-source rejected before the dry-run return (dry-run/real parity); `scope.py` clean ScopeError on unreadable dirs, GUI won't silently clobber a recipe, `relink --create-missing` warns on unmapped projects. **LOW:** jcampdx `##END` junk-key, DICM-magic gate, deterministic microscopy pick, `primary_kind` AUTO, README date fallback, provenance FILE-NNNN lock, multi-dot `.tar.gz` archive match, `ni_ingest` subprocess, `ni_live_discover` parent guard, migrate message; atomic temp-file writes (`create_project` / `recover_subject_metadata` / `ftp_mirror`). Also hardened `relink_axioscan_collisions.py` (keep the date-less link unless a dated replacement exists). Tests: `tools/test_review_fixes_2026_06.py` + a stale-break-spares-fresh-lock case in `test_locking.py`; full suite green. |
| 2026-06-14 | R. Tasseff | **Microscopy (AxioScan) anatomy auto-derive + back-fill — mirrors the MRI work.** AxioScan `.czi` sample-ids encode the organ in the suffix (`ID12Lu`→lung, `ID29H`→heart) but the ingest discarded it. Now `anatomy_derive.derive_microscopy_anatomy` maps the suffix → UBERON `region` via an **operator-keyed reference map** `tools/reference/microscopy_organ_map.yaml` (data, not code — the vocabulary differs by operator: AUA `Lu/Li/K`, MBC `H/B/L/HL`; `L`=lung confirmed with MBC; AUA `T`=tumor intentionally null), wired into `enrichment._build_anatomy` (tissue path, operator-set wins, high-confidence only) + a back-fill tool `tools/backfill_microscopy_anatomy.py`. Consolidated operator runbook for both instruments: [`tools/ANATOMY_BACKFILL.md`](tools/ANATOMY_BACKFILL.md) (incl. the Jesús-group one-time MRI override). Dry-run on the live ZWSI set: ~88/146 fill (heart 57, lung 31). UBERON ids OLS-verified. Shipped isolated to `main`. See [08_METADATA §4.6.4](mfb-rdm-docs/08_METADATA.md). |
| 2026-06-14 | R. Tasseff | **MRI anatomy auto-derive + back-fill (mapping finalized with the MRI lead).** So the bulk historical 7T ingest doesn't need every scan's anatomy hand-labelled in YAML, the ingest now auto-fills `anatomy.region` (UBERON) + `is_whole_body` from the **scan name** when the operator left anatomy unset — new `tools/ingest/anatomy_derive.py` (`ANATOMY_RULES`), wired into `enrichment._build_anatomy` (`ingest_raw.py` Step 8.4), plus `tools/backfill_mri_anatomy.py` for already-ingested sidecars (dry-run default; updates sidecar + registry `anatomical_entity`). Mapping reviewed with J. Ruiz-Cabello (2026-06-14): **high-confidence literal terms only, null if any doubt, never guess** — heart (4-chamber/long-axis/short-axis/cardiac/ventricle), large vessels **only when named** (MPA→pulmonary artery, aorta, carotid), brain, abdomen; setup/planning scans skip; bare cine / unnamed velocity-map / FLASH-RARE / FOV → null. UBERON ids verified via EBI OLS. No group-specific assumptions baked in (system will expand beyond cardiac). Shipped isolated to `main` off the review branch; see [08_METADATA §4.6.4](mfb-rdm-docs/08_METADATA.md). |
| 2026-06-12 | R. Tasseff | **MRI no-DICOM regeneration — official operator runbook + env tooling.** For the historical MRI pull: when a ParaVision exam arrives without DICOMs (researcher skipped Bruker's exporter), the ingest regenerates them from `2dseq`+JCAMP-DX via Dicomifier (in WSL) with the two confirmed PV-7 image fixes (PixelSpacing axis-swap + Window-tag), then deposits into `/raw/`. Wrote the canonical step-by-step runbook ([`equipment/mri-platform/mri_no_dicom_regeneration_runbook.md`](equipment/mri-platform/mri_no_dicom_regeneration_runbook.md)); committed a reproducible conda env spec ([`tools/dicomifier-pilot.environment.yml`](tools/dicomifier-pilot.environment.yml)); made `validate_dicomifier_pixelspacing.py` operator-usable (takes an `<ACQ-ID>.data/` path, PASS/FAIL on swapped-axis). The conversion code (`paravision_regen.py` + the `auto_regenerate_dicom` ingest flag) already existed + was visually verified 2026-06-01 — this turn makes it findable + runnable by the team. Cross-linked from `equipment/INDEX.md`, `INGEST_CLI.md`, `tasks.md §3.1`. |
| 2026-06-12 | R. Tasseff | **NI live-sync reconciliation + the `subject_ids` rename.** Reviewed the designer's NI Step-1 discovery (`ni_live_discover.py`), added the typo-shaped `project-conflict` flag + a 30-check grammar test, and fixed a doc contradiction (D1) that would have built a vetoed `(acq_id, animal)` junction table — `registry_subjects.csv` is now spec'd as **one row per subject** everywhere. Then renamed the AUTO registry column **`subject_id` → packed `subject_ids`** (`;`-joined, always-a-list; NI-LIVE-08): `registry.py`/`resolver.py`/`build_row`/tests, made `migrate_registry_columns.py` rename-aware (`COLUMN_RENAMES`), and migrated the sandbox header (28 cols; the purged production registry is born with it). Multi-animal list-packing + the subjects-table writer stay the designer's Lane-A/B. See [ni_live_step2_plan.md](tasks/archive/ni_live_step2_plan.md) + [ni_live_sync_handoff.md](tasks/archive/ni_live_sync_handoff.md). |
| 2026-06-12 | R. Tasseff | Merged the correction pass into the designer's true-production-restart `origin/main`: consolidated the registry schema (adopted the 3-column `sample_organism`+`subject_id`+`anatomical_entity` `build_row(subject=, anatomy=)` projection over S1's single column; one copy of the cross-drive relpath fix; kept all the correction-pass integrity work). Decided the NI multi-animal shape — **NI-LIVE-08:** packed `subject_ids` + a true one-row-per-subject `registry_subjects.csv` + minimal sidecar; no mapping table now (deferred query layer). Switched `origin` to the SSH remote so the clone pushes headless. See [origin_main_merge_review.md](tasks/archive/origin_main_merge_review.md). |
| 2026-06-11 | R. Tasseff | Post-first-review correction pass: registry lock + durable ACQ-ID reservation and BOM/newline-safe CSV appends (concurrency + Excel integrity); archive re-run idempotency, crash-after-copy rollback (registry append = commit point) and empty-folder fail-loud fixes; cross-drive relpath guard; the auto `subject_id` registry column (S1); dry-run-default-ON microscopy GUI with a loud banner + end-of-run summary; CELL + LSM9 seed recipes; the `os.link` MRI/NI diagnostic; the frozen GUI exe path fix; and the doc-clarity front-doors (`START_HERE.md` / `GLOSSARY.md` / `CHANGELOG.md`). See [06_REGISTRIES §2.7](mfb-rdm-docs/06_REGISTRIES.md). |
| 2026-06-10 | R. Tasseff | True-production restart begun: quasi-prod data on `J:\gjesus3-data` purged (folder skeleton + ACLs + `recipes/` kept; CSVs backed up off-NAS); the fresh registry header gained three Auto columns — `sample_organism` + `subject_id` + `anatomical_entity` (projections of the enrichment blocks via `registry.build_row`), 28 cols, no migration. Plus operator-GUI builder "start from an existing recipe" + Windows-SMB default NAS root; `operator-ingest-tooling` merged to `main`. See [06_REGISTRIES §2](mfb-rdm-docs/06_REGISTRIES.md). |
| 2026-06-09 | R. Tasseff | `operator` re-added as a registry column alongside `researcher` (live header migrated 24→25 cols) so operators can find their own scans without opening sidecars, plus microscopy GUI Phase-1 operator-feedback fixes. See [06_REGISTRIES §2.3a-bis](mfb-rdm-docs/06_REGISTRIES.md). |
| 2026-06-09 | R. Tasseff | Person/role naming fixed globally — registry `operator`→`researcher`, `operator` now sidecar-only, permission roles renamed Operator→Tech / User→Researcher. See [06_REGISTRIES §2.3a-bis](mfb-rdm-docs/06_REGISTRIES.md). |
| 2026-06-09 | R. Tasseff | NI no longer records the wrong PI from `protocol.txt`, MRI parses `discovered.pi_initials` and aligns registry `sample_id` to NI's `m<animal>_<project>`, plus new `tools/reference/pi_group_lookup.yaml` and `tasks/BACKLOG.md`. See [09_MODALITIES](mfb-rdm-docs/09_MODALITIES.md). |
| 2026-06-09 | R. Tasseff | `anatomy:` block extended to ex-vivo `tissue` (region-only, same UBERON `region` field as in-vivo coverage) and a REMBI miscitation for `anatomical_entity` fixed. See [08_METADATA §4.6](mfb-rdm-docs/08_METADATA.md). |
| 2026-06-08 | R. Tasseff | Operator ingest front-ends verified and `condition:`/`anatomy:` capture added at the CLI (`--is-control` / `--disease-model` / `--disease-state` / `--is-whole-body` or prompts, set once per run, gate-aware WARNs) with control DEFINED. See [08_METADATA §4.5](mfb-rdm-docs/08_METADATA.md). |
| 2026-06-03 | R. Tasseff | Phase 3 preclinical enrichment writer IMPLEMENTED — `subject:`/`condition:`/`anatomy:` sidecar blocks written non-blocking at ingest Step 8.4 via `tools/ingest/enrichment.py`, plus five new standalone tools and a deferred-recovery pending list. See [08_METADATA §4.7](mfb-rdm-docs/08_METADATA.md). |
| 2026-06-03 | R. Tasseff | Non-blocking metadata model adopted — enrichment never blocks ingest; `is_control` / `is_whole_body` become highly-recommended tri-state (`true`/`false`/`null`), set once per batch and best-effort auto-filled. See [08_METADATA §4.7](mfb-rdm-docs/08_METADATA.md). |
| 2026-06-03 | R. Tasseff | New `anatomy:` sidecar block for in-vivo scans (`sample_type = organism`) — a whole-body-vs-ROI `is_whole_body` flag plus UBERON-coded `region`, operator-entered (not auto-derivable). See [08_METADATA §4.6](mfb-rdm-docs/08_METADATA.md). |
| 2026-06-03 | R. Tasseff | Animal-facility DB explored and the Subject/Sample two-tier identity model adopted (Option B) — `subject.facility_animal_id` reused verbatim as the subject id, registry columns deferred to true production. See [06_REGISTRIES §2.3](mfb-rdm-docs/06_REGISTRIES.md). |
| 2026-06-02 | R. Tasseff | Animal-facility-DB access obtained — `subject:` block extended with DB-sourced `date_of_birth` (age now derived) plus the §4.4.6 deferred-recovery subsystem for DB-miss / no-credentials ingests. See [08_METADATA §4.4](mfb-rdm-docs/08_METADATA.md). |
| 2026-06-02 | R. Tasseff | Project links switched from Windows `.lnk` shortcuts to NTFS/SMB hard links via `linker.create_hardlink` and `ingest_raw.py` Step 12; all 284 existing `.lnk` migrated in place. See [10_TOOLS §2.1.1](mfb-rdm-docs/10_TOOLS.md). |
| 2026-06-02 | R. Tasseff | NAS group permissions APPLIED on `gjesus3-data` using the existing `GJesus` group (operators write-but-not-modify on `raw\`, grant-only never DENY); one operator-account verification pending. See [11_OPERATIONS §2.1.1](mfb-rdm-docs/11_OPERATIONS.md). |
| 2026-06-02 | R. Tasseff | gjesus3-data container rebuild — whole exhibit dataset (462 acqs) re-ingested from YAML configs into a single top-level container, plus `acquisition_datetime` StudyDate fallback and `acquisition_layout: archive` implemented. See [10_TOOLS](mfb-rdm-docs/10_TOOLS.md). |
| 2026-05-29 | R. Tasseff | DRAFT `condition:` block spec — a top-level disease-state/control block for `sample_type ∈ {organism, tissue}` with `is_control` as the enforceable healthy-vs-case flag. See [08_METADATA §4.5](mfb-rdm-docs/08_METADATA.md). |
| 2026-05-29 | R. Tasseff | Dicomifier (`bruker2dicom`) identified as the open-source FID→DICOM regeneration candidate, revising the earlier 2-4 week build-from-scratch estimate; PV 7 validation needed. See [`tasks/archive/tasks.md §3.1`](tasks/archive/tasks.md). |
| 2026-05-29 | R. Tasseff | DRAFT `subject:` block spec with four DECIDED required fields (species/strain/sex/age) plus a 4-phase animal-facility-DB integration work stream. See [08_METADATA §4.4](mfb-rdm-docs/08_METADATA.md). |
| 2026-05-27 | R. Tasseff | Round-6 v2 internal MRI redo — new `copy_mri_paravision` selective-copy path mirroring NI v2.1 (slim `<ACQ-ID>.data/` layout, JCAMP-DX parsed-only, `reconstructions: all`) plus no-DICOM acquisition handling. See [09_MODALITIES §1.4](mfb-rdm-docs/09_MODALITIES.md). |
| 2026-05-27 | R. Tasseff | Round-8 NI v2.1 — platform-generated multi-frame `frameMULTI` DICOMs now kept alongside per-frame under `<ACQ-ID>.data/`, distinguishable by `ImageType`/`NumberOfFrames`. See [09_MODALITIES §1.5](mfb-rdm-docs/09_MODALITIES.md). |
| 2026-05-27 | R. Tasseff | Round-8 NI v2 — operator-feedback reshape (slimmer on-disk shape, `<ACQ-ID>.data/` rename, flat DICOM names), DICOM UID capture in curated tags, and a `linker.py` UNC-normalization fix. See [09_MODALITIES §1.5](mfb-rdm-docs/09_MODALITIES.md). |
| 2026-05-26 | R. Tasseff | Round-8 NI REDO — fixed a bug that copied ~358 GB of platform raw with zero DICOMs by adding a dedicated `copy_ni_acquisition` and `ni_metadata.py` extractor, plus captured the not-the-archive reframe. See [13_GJESUS3_ROLE §5.6](mfb-rdm-docs/13_GJESUS3_ROLE.md). |
| 2026-05-22 | R. Tasseff | Round 8 — Nuclear Imaging (PET/CT) archive-mode landed in quasi-production (84 acqs, zero new projects via cross-modality reuse), with the archive name parsed for all registry metadata. See [`tasks/archive/tasks.md §4.7`](tasks/archive/tasks.md). |
| 2026-05-22 | R. Tasseff | Round 7 — Zeiss LSM 900 confocal (LSM9) landed in quasi-production (13 `.czi` acqs, zero new code) reusing `czi_metadata.py` with a new `lsm900.yaml` template. See [09_MODALITIES §1.3](mfb-rdm-docs/09_MODALITIES.md). |
| 2026-05-22 | R. Tasseff | Round 6 — internal MRI (Bruker ParaVision) landed in quasi-production (97 exams, no-zip folder-as-primary, `mri:` sidecar block) plus the new `link_filename:` YAML field for unique `.lnk` names. See [10_TOOLS §2.1.5](mfb-rdm-docs/10_TOOLS.md). |
| 2026-05-18 | R. Tasseff | Round 5 — Cell Observer (CELL) landed in quasi-production (165 `.czi` acqs, 3 auto-created projects), the canonical exhibit showing one pipeline handle filename- vs path-focused metadata by YAML alone. See [`tasks/archive/tasks.md §4.6.B`](tasks/archive/tasks.md). |
| 2026-05-14 | R. Tasseff | Round-5 architectural prep — new optional `path_parse:` and `auto_create_project:` YAML blocks documented, plus the project-naming consensus open question (PROJ-05). See [10_TOOLS §2.1.3](mfb-rdm-docs/10_TOOLS.md). |
| 2026-05-13 | R. Tasseff | Round-5 prep + context-switching scaffolding — Cell Observer workflow notes committed, `tasks.md` restructured with a §0 "Active Pass / Up Next", and the `path_parse` design intent locked in. See [`equipment/cell-observer/`](equipment/cell-observer/). |
| 2026-05-12 | R. Tasseff | Provenance auto-logging on ingest — adopted the rule that any tool changing files under `/projects/`, `/publications/`, or curated datasets updates provenance, via the new `tools/ingest/provenance.py`. See [07_PROVENANCE §2.1](mfb-rdm-docs/07_PROVENANCE.md). |
| 2026-05-12 | R. Tasseff | Metadata-location architecture decision — acquisition-level metadata stays immutable in `/raw/`, study-level researcher metadata moves to mutable `/projects/<proj>/metadata/`, plus the new `User` role. See [08_METADATA §1](mfb-rdm-docs/08_METADATA.md). |
| 2026-05-12 | R. Tasseff | Round-4 fixes from user handoff testing — fail-fast on invalid `--nas-root`, Windows path examples, decimal-MB `file_size_mb`, per-instrument template structure, and the DRAFT `sample_type` vocabulary. See [06_REGISTRIES §2.3](mfb-rdm-docs/06_REGISTRIES.md). |
| 2026-05-12 | R. Tasseff | Round-4 AxioScan 7 validation — 28-acquisition test ingest (3 animal projects, 1 config, auto-create-then-reuse) adopting the composite `sample_id` and `ae-biomegune-NNNN` project conventions via YAML interpolation. See [`tools/INGEST_CLI.md`](tools/INGEST_CLI.md). |
| 2026-05-06 | R. Tasseff | AxioScan 7 (.czi) ingest end-to-end with the new three-block YAML config schema (`ingest:`/`auto_discover:`/`registry:`) replacing `defaults:`, plus `.czi`-internal metadata extraction (21 `czi_*` fields + `microscopy:` block). See [10_TOOLS §2.1](mfb-rdm-docs/10_TOOLS.md). |
| 2026-05-05 | R. Tasseff | Project linking method resolved — Windows `.lnk` shell shortcuts (pilot-specific Windows-first choice), linker implemented in `tools/ingest/linker.py`. See [10_TOOLS §2.1.1](mfb-rdm-docs/10_TOOLS.md). |
| 2026-03-06 | R. Tasseff | DICOM stored as compressed archives, primary staging off-NAS, two ingest modes (full + lightweight), and the one-primary-file rule simplified (no DICOM exception). See [00_INDEX](mfb-rdm-docs/00_INDEX.md) Key Decisions. |
| 2026-03-02 | R. Tasseff | Promoted the Projects area to a live feature (Draft) and removed it from deferred. See [05_PROJECTS](mfb-rdm-docs/05_PROJECTS.md). |
| 2026-02-25 | R. Tasseff | Raw structure changed to ecosystem-based (MICROSCOPY/DICOM/EM), resolving RAW-05, plus the new 12_CURATED_DATASETS spec. See [03_RAW_STORAGE](mfb-rdm-docs/03_RAW_STORAGE.md). |
| 2026-02-02 | R. Tasseff | Restructured from a monolithic spec into modular numbered documents and refocused on archival scope. See [00_INDEX](mfb-rdm-docs/00_INDEX.md). |
| 2025-01-22 | R. Tasseff | v0.2 — added discussion flags for the stakeholder meeting. See [00_INDEX](mfb-rdm-docs/00_INDEX.md). |
| 2025-01-22 | R. Tasseff | v0.1 — initial comprehensive draft. See [00_INDEX](mfb-rdm-docs/00_INDEX.md). |
